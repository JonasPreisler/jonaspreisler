<%= form_with(model: misschili, local: true) do |form| %>
  <% if misschili.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(misschili.errors.count, "error") %> prohibited this misschili from being saved:</h2>

      <ul>
      <% misschili.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
